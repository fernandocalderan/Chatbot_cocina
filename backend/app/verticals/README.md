# Vertical registry

This registry defines the admin-controlled verticals and their default flow mapping.

Notes:
- Tenants without `vertical_key` keep legacy behavior (plan-based flows).
- TODO: enforce vertical assignment on all new tenants and add migration for legacy tenants.
